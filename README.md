A Python-based post search engine built with Elasticsearch. It preprocesses and indexes textual data, then provides Boolean queries, phrase matching, numeric and date filters, similarity search, and a simple GUI for exploring the indexed posts.
(The data.csv file with the Twitter posts of Donald Trump isn't included)
